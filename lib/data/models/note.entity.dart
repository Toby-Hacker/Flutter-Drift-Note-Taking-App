import 'package:drift/drift.dart';
import 'package:drift_note_taking_app/data/local/app_database.dart';

// void f() async {
//   final db = AppDatabase.instance();
//   final result = await (db.select(db.userNote)
//         ..where((table) => table.id.equals(1)))

//       .get();

//       deleteDatabase

      

      
// }
